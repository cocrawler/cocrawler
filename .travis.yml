#to get 14.04:
#sudo: required
#dist: trusty

language: python
python:
  - 3.5
  - 3.6
  - 3.7-dev  ### https://github.com/aio-libs/aiohttp/issues/1839, fixed but not in shipping version
  - nightly

matrix:
  fast_finish: true
  allow_failures:
  - python: 3.7-dev
  - python: nightly
  include:
  - python: 3.5.0
    env: COCRAWLER_LOGLEVEL=3

install: "pip install -r requirements.txt"

script:
  - make test_coverage
after_success:
  coveralls
